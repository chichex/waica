{
  "name": "@chichex/waica",
  "version": "0.2.0",
  "description": "Waica in one command — `npx @chichex/waica` opens the editor in your browser",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/chichex/waica",
    "directory": "packages/cli"
  },
  "bin": {
    "waica": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node bundle-editor.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@waica/editor": "workspace:^"
  }
}
